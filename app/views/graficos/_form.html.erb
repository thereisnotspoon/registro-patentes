<%= form_for(@grafico) do |f| %>
  <% if @grafico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grafico.errors.count, "error") %> prohibited this grafico from being saved:</h2>

      <ul>
      <% @grafico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :grafico %><br>
    <%= f.text_area :grafico %>
    <%= f.file_field :grafico%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
